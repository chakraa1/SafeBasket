# SafeBasket backend - core runtime dependencies.
# These are intentionally lightweight so the service installs fast and runs
# fully offline. The FAISS RAG layer uses a deterministic hashing embedding by
# default; install requirements-ml.txt to upgrade to sentence-transformers.

fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.31
pydantic>=2.6,<3
python-multipart>=0.0.9

numpy>=1.26,<2.1
pillow>=10.2
pytesseract>=0.3.10

faiss-cpu>=1.8.0
langchain>=0.2.10,<0.3
langchain-core>=0.2.20,<0.3
langchain-community>=0.2.10,<0.3
langchain-openai>=0.1.20,<0.2

# Testing
httpx>=0.27
pytest>=8.0
